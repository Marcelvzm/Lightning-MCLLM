# TODO

Ad-hoc near-term work. Architectural roadmap lives in `technical_details.md` §19.

## Short-term

- [ ] **`setup.sh` für macOS & Linux** — One-liner Setup: venv anlegen, deps installieren (mit `[mcp]` und `[audio]` extras optional via flags), Smoke-Test laufen lassen, Hinweise auf Hardware-Setup ausgeben. Variante für Windows als `setup.ps1` separat.
- [ ] Erste echte Hardware-Validierung mit Eurolite USB-DMX512 PRO MK2 (Baud 57600 → ggf. 115200/250000 nachziehen, "Pro RX/TX" Mode prüfen).

name: bar_blue
description: Bar-Heads blue, full bright.

parameters:
  gobo: { type: int, default: 0, min: 0, max: 255 }

targets:
  - select: { tag: bar }
    palette: { name: blau, facet: wheel }
    values: { dimmer: 255, gobo/wheel: "${gobo}", strobe: 0 }

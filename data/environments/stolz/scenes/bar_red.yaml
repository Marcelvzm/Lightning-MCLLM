name: bar_red
description: Bar-Heads rot, full bright. Position bleibt wie sie ist.

parameters:
  gobo: { type: int, default: 0, min: 0, max: 255 }

targets:
  - select: { tag: bar }
    palette: { name: rot, facet: wheel }
    values: { dimmer: 255, gobo/wheel: "${gobo}", strobe: 0 }
